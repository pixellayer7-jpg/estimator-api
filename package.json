{
  "name": "estimator-api",
  "private": true,
  "version": "0.1.0",
  "description": "PixelLayer — minimal API to persist and share project quotes (MVP, file-backed).",
  "type": "module",
  "author": "PixelLayer L.L.C",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0"
  }
}
